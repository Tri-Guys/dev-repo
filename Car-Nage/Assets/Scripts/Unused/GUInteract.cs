using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class GUInteract : MonoBehaviour
{

    //TextMeshPro playButton = PLAY.GetComponent<TextMeshPro>();
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        void onMouseOver()
        {
            //PLAY.color = new Color32(128, 128, 128, 128);
        }
    }
}